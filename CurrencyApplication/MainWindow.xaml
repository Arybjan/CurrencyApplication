<Window x:Class="CurrencyApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Currency App" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Background="#F3F3F3" Grid.Column="0">
            <TextBlock Text="Currency App"
                       FontSize="22"
                       FontWeight="Bold"
                       Margin="20"/>

            <Button Content="Список валют"
                    Margin="10"
                    Height="40"
                    Click="CurrenciesButton_Click"/>

            <Button Content="Добавить валюту"
                    Margin="10"
                    Height="40"
                    Click="AddCurrencyButton_Click"/>
        </StackPanel>

        <Frame x:Name="MainFrame"
               Grid.Column="1"
               NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>