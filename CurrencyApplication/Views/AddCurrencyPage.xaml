<Page x:Class="CurrencyApplication.Views.AddCurrencyPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="AddCurrencyPage">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                       Text="Добавление валюты"
                       FontSize="24"
                       FontWeight="Bold"
                       Margin="0,0,0,20"/>

            <TextBlock Grid.Row="1" Grid.Column="0"
                       Text="Id:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,10"/>
            <TextBox Grid.Row="1" Grid.Column="1"
                     Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10"/>

            <TextBlock Grid.Row="2" Grid.Column="0"
                       Text="CharCode:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,10"/>
            <TextBox Grid.Row="2" Grid.Column="1"
                     Text="{Binding CharCode, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10"/>

            <TextBlock Grid.Row="3" Grid.Column="0"
                       Text="Название:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,10"/>
            <TextBox Grid.Row="3" Grid.Column="1"
                     Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10"/>

            <TextBlock Grid.Row="4" Grid.Column="0"
                       Text="Номинал:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,10"/>
            <TextBox Grid.Row="4" Grid.Column="1"
                     Text="{Binding Nominal, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10"/>

            <TextBlock Grid.Row="5" Grid.Column="0"
                       Text="Курс:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,10"/>
            <TextBox Grid.Row="5" Grid.Column="1"
                     Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10"/>

            <TextBlock Grid.Row="6" Grid.Column="0"
                       Text="Предыдущий курс:"
                       VerticalAlignment="Center"
                       Margin="0,0,10,10"/>
            <TextBox Grid.Row="6" Grid.Column="1"
                     Text="{Binding Previous, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,0,10"/>

            <StackPanel Grid.Row="7" Grid.ColumnSpan="2"
                        Orientation="Horizontal"
                        Margin="0,20,0,0">
                <Button Content="Сохранить валюту"
                        Width="180"
                        Height="40"
                        Command="{Binding SaveCurrencyCommand}"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>